extends layout
block content
    title= title
    a(href='/character/create') Create new character
    ul
        each character in characters
            li
                a(href=character.url) #{character.name}
        else 
            li There aren't characters
