extends layout

block content
    h1 #[strong Figure:] #{figure.name}
    ul
        li #[strong Character:] #{figure.character.name}
        li #[strong Franchise:] #{figure.character.franchise.name}

